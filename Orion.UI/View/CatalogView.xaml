<UserControl x:Class="Orion.UI.View.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Orion.UI.View"
             mc:Ignorable="d" 
             xmlns:ctrl="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             dialog:DialogParticipation.Register="{Binding}"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks" 
             xmlns:vm="clr-namespace:Orion.UI.ViewModel"
             xmlns:view="clr-namespace:Orion.UI.View"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:CatalogViewModel}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition MinWidth="400" MaxWidth="400"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderThickness="1" Margin="5" BorderBrush="{StaticResource MahApps.Brushes.Accent}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ProductsActive, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            
            <StackPanel Orientation="Vertical">
                <Expander Header="QUANTECH" Margin="5" Width="120" IsExpanded="True">
                    <StackPanel Orientation="Vertical">
                        
                        <Button Margin="1" Background="DarkSlateBlue" Padding="0" Command="{Binding CatalogA1Command}">
                            <Button.Content>
                                <TextBlock Text="Air Cooled Chillers Factory Lead Time" TextWrapping="Wrap"/>
                            </Button.Content>
                        </Button>

                        <Button Margin="1" Background="DarkSlateBlue" Padding="0" Command="{Binding CatalogA2Command}">
                            <Button.Content>
                                <TextBlock Text="Air Cooled Chillers Stock Non Coated Condenser"  TextWrapping="Wrap"/>
                            </Button.Content>
                        </Button>

                        <Button Margin="1" Background="DarkSlateBlue" Padding="0" Command="{Binding CatalogA3Command}">
                            <Button.Content>
                                <TextBlock Text="Air Cooled Chillers Stock Post Coated Condenser"  TextWrapping="Wrap"/>
                            </Button.Content>
                        </Button>

                        <Button Margin="1" Background="DarkSlateBlue" Padding="0" Command="{Binding CatalogA4Command}">
                            <Button.Content>
                                <TextBlock Text="Water Cooled Chillers Factory Lead Time"  TextWrapping="Wrap"/>
                            </Button.Content>
                        </Button>

                    </StackPanel>
                </Expander>
            </StackPanel>
        </Border>


        <Grid Grid.Column="1">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Grid.ColumnSpan" Value="2"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ProductsActive, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Value="False">
                            <Setter Property="Grid.ColumnSpan" Value="2"/>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding EditActive, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Value="True">
                            <Setter Property="Grid.ColumnSpan" Value="1"/>
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Grid>

        <ContentControl Content="{Binding EditViewModel}" Grid.Column="2"/>
    </Grid>
</UserControl>
